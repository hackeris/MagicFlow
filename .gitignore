# ── build outputs ──
/node_modules
/oh_modules
/.hvigor
**/build
**/.cxx

# ── local env / IDE ──
/local.properties
/.idea
/.clangd
/.clang-format
/.clang-tidy
/.appanalyzer

# ── DevEco template test & preview mocks (not part of Phase 0 deliverable) ──
/entry/src/test
/entry/src/ohosTest
/entry/src/mock

# ── local signing keys / scratch (do not commit) ──
/.ohos
/entry/libs/*/*.so*
.temp
.superpowers

# ── 复现链产物（不入库; 由 fetch/collect/make 重建并做锚校验）──
# patch 生成用临时工作树（带 .git; 非交付物, 生成 patch 的正规源 = externals/comfyui-src 的 git 树）
/patches/working/
/externals
/entry/src/main/cpp/prebuilt
/entry/src/main/resources/rawfile/python312.zip
/stub/__pycache__
/scripts/__pycache__
