# ── poc/npu 构建产物与缓存(提交清单见 README / 会话清点) ──────────────
# 需要提交的源码在: build_and_run.sh、AppScope/、entry/src/、工程配置文件、rawfile/custom_graph.omc

# 构建产物
entry/build/

# 依赖安装生成
oh_modules/
entry/oh_modules/

# hvigor / CMake 缓存
.hvigor/
entry/.hvigor/
.cxx/
entry/.cxx/
CMakeCache.txt
CMakeFiles/
*.ninja
.ninja_deps
.ninja_log

# HAP 等二进制产物(兜底)
*.hap
*.har
*.hsp

# DevEco 生成本地配置
local.properties

# 临时/日志
*.log
*.tmp
